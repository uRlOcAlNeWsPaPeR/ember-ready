# Copy this file to backend/.env and fill in real values.
# backend/.env is gitignored — never commit your real key.
#
# Optional. Powers the two "✨ Ask AI to explain this" / "✨ Ask AI to
# summarize my plan" buttons (see backend/app/api/ai_explain.py). Get a
# free key at https://aistudio.google.com/apikey — everything else in
# EmberReady works with zero API keys.
GEMINI_API_KEY=
